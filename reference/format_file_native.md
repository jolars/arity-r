# Format one UTF-8 file in place and report whether it changed.

Format one UTF-8 file in place and report whether it changed.

## Usage

``` r
format_file_native(
  path,
  line_width,
  indent_width,
  line_ending,
  roxygen_markdown,
  verify
)
```
